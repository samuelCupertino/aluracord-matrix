import { CardLogin } from "../..";
import { SpaceBackground } from "../../atoms";
import { LoginContainer } from "./styles";

export const Login = () => {
  return (
    <LoginContainer>
      <SpaceBackground />
      <CardLogin />
    </LoginContainer>
  );
};
